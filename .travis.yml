sudo: required

services:
  - docker

language: java

jdk:
 - oraclejdk8

os:
  - linux

cache:
  directories:
  - $HOME/.gradle

before_script: 
 - docker-compose up --build -d
  		  
script:
 - ./gradlew check
 - ./gradlew codeCoverageReport
 - ./test.sh

after_script:
 - docker-compose down

after_success:
 - bash <(curl -s https://codecov.io/bash)
